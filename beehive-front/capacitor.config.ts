import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'bumblebee-guard',
  webDir: 'dist/bumblebee-guard/browser',
};

export default config;
